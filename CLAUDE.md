# CLAUDE.md

Use `skills/data-engineering/SKILL.md` as the canonical contract. Detailed
knowledge is progressively disclosed from its direct `references/` links;
templates and schemas are in `assets/`; deterministic utilities are in `scripts/`.

The supported workflows are `GUIDE`, `DESIGN`, `BUILD`, `REVIEW`, `OPERATE`, and
`MODERNIZE`. Keep provider behavior in `integrations/` or thin adapter metadata.
Run `tests/run_e2e_harness.sh` before shipping changes.
